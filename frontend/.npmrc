# Ensure npm scripts can spawn reliably from Windows/MSYS automation shells.
# Without this, npm can fail before running scripts when COMSPEC/script-shell is unset.
script-shell=C:\Windows\System32\cmd.exe
